<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cz.zcu.kiv.crce</groupId>
        <artifactId>compiled-bundle-settings</artifactId>
        <version>2.1.2-SNAPSHOT</version>
    </parent>

    <groupId>cz.zcu.kiv.crce.wrapper</groupId>
    <artifactId>com.datumbox.lpsolve</artifactId>
    <version>5.5.2.3</version>
    <packaging>bundle</packaging>

    <name>${bundle.symbolicName} ${version} [osgi]</name>

    <properties>
        <bundle.symbolicName>com.datumbox.lpsolve</bundle.symbolicName>
        <bundle.namespace>lpsolve</bundle.namespace>
        <wrapped.groupId>com.datumbox</wrapped.groupId>
        <wrapped.artifactId>lpsolve</wrapped.artifactId>
        <wrapped.version>5.5.2.3</wrapped.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>${wrapped.groupId}</groupId>
            <artifactId>${wrapped.artifactId}</artifactId>
            <version>${wrapped.version}</version>
            <scope>compile</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>findbugs-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
